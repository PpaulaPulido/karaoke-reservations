import { 
    initNavbarScroll, 
    initRecommendationCards, 
    initExperienceSection, 
    initHeroSection 
} from './functions.js';

document.addEventListener('DOMContentLoaded', function () {
    initRecommendationCards();
    initExperienceSection();
    initHeroSection();
});
